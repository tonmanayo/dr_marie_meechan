import type { LetterTopic } from "./queries";

/** Display labels for letter topics — shared by the index mapper and the letter page. */
export const TOPIC_LABELS: Record<LetterTopic, string> = {
  loss: "Loss & grief",
  beyond: "Beyond fertility",
};
